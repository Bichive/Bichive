function checkUrl(w, b) {
  this.w = window;
  this.b = w.location.href;
  if (b.includes("bichive.github.io")) {
    alert("Dev: testing-website...");
    r = prompt("What is your status?");
    if (prompt = 'bichive') {
      alert("Well; then welcome to the developer version of our website!");
    }
  }
}

checkUrl();
